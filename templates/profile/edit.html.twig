{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-2xl font-bold mb-4">Éditer le Profil</h1>

    {{ form_start(form) }}
    <div class="space-y-4">
        {{ form_row(form.name, {'attr': {'class': 'input w-full p-2 border rounded'}}) }}
        {{ form_row(form.email, {'attr': {'class': 'input w-full p-2 border rounded'}}) }}
    </div>
    <button type="submit" class="mt-4 px-4 py-2 bg-emerald-500 text-white rounded">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}